__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
.venv/
venv/
dist/
build/
*.whl
.DS_Store
.ruff_cache/

# Validation set: real PDFs + ground-truth labels are large/sensitive, kept out of git.
# labels.example.yaml is the format reference and IS committed.
validation_set/pdfs/
validation_set/labels.yaml
validation_set/scorecard.json

# Pending manual-label TSV (regenerated by validation harness)
validation_set/labels-pending.tsv


# v0.2.a execution artifacts (captured in PR comment, not committed)
docs/plans/_exec_re_extract_with_log.py
docs/plans/v0_2_a_rejection_log.txt

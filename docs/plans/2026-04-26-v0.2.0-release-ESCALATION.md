# ESCALATION — Unit 5 v0.2.0 release (Option B)

## Phase 0.a Classification

**fast-path-eligible** — preserved.

## 1. Iter-1 findings (7 — escalating per established pattern)

1. Regen step unsafe (no dir-clear, exception-skip, count-only check)
2. Corpus-size inconsistency in plan (30/27/31 mixed)
3. Manual diff transcription bypasses regression-detection rigor
4. NEW_DOCS_SINCE_PREFIX too narrow (additions on existing PDFs need separate handling)
5. batch1 inversion lacks gating-on-survival decision
6. Wheel artifact not verified as installable
7. "Publication" scope ambiguous (repo only? PyPI?)

## 2. Recommended Option B execution

Apply 7 corrections to the iter-1 design before hand-writing:

### A. Regen safety (Codex #1, #2)
- Clear `validation_set/candidate-output/*.json` first (not directory delete; preserve `NO_DEFINITIONS.txt`)
- Loop fails hard on any analyze_pdf exception (no silent skip)
- Verify resulting file set against an explicit manifest derived from `validation_set/pdfs/`

Expected post-regen inventory:
- 30 PDFs in `validation_set/pdfs/` (verified via Sub-Unit 1a audit)
- 1 PDF that's a non-`.pdf` truncated-extension file (`AR_215-1_..._OCR.p`) — analyze_pdf can still handle if pymupdf accepts; but file-stem matching must work
- 2 extensionless files — same handling
- For each PDF: run analyze_pdf; emit JSON if entries OR if metadata indicates analysis ran (i.e., always emit JSON)
- 0-entry case: still emit JSON (the metadata is the value)
- Final count: 30 JSONs (one per PDF, regardless of entries)

### B. Diff artifact committed (Codex #3)
- Generate `validation_set/v0.2.0_classifier_snapshot_diff.md` showing every removed + added key with verdict
- Commit alongside test changes
- The diff IS the review artifact — it's what the test asserts against

### C. Separate handling for additions on existing vs new PDFs (Codex #4)
- `NEW_DOCS_SINCE_PREFIX_PDFS` = set of 4 new PDF stems (Unit 4 additions)
- Allowed `added` keys split by PDF:
  - If PDF in NEW_DOCS_SINCE_PREFIX_PDFS → key allowed without per-key listing (whole new doc is expected)
  - If PDF was in prefix but new key shows up → must be in explicit `ADDED_ON_EXISTING_PDFS` set with justification

### D. batch1 inversion gating (Codex #5)
- Pre-step: regenerate ONLY TC 1-19.30 + FM 3-34 candidate-output and check the forbidden pairs
- If either pair still present in regenerated output: HALT Unit 5; do not invert; file follow-up plan
- If both pairs absent: invert assertion in `test_batch1_reconciled.py` per its docstring lifecycle

### E. Wheel installable smoke-test (Codex #6)
- `python -m build --wheel` (clean — `rm -rf build/ *.egg-info` first)
- `python -m venv /tmp/wheel-test-env`
- `/tmp/wheel-test-env/bin/pip install dist/fedresearch_dictionary_extractor-0.2.0-py3-none-any.whl`
- `/tmp/wheel-test-env/bin/extract-definitions --version` → expect `0.2.0`
- `/tmp/wheel-test-env/bin/python -c "import fedresearch_dictionary_extractor; print(fedresearch_dictionary_extractor.__version__)"` → `0.2.0`

### F. Publication scope clarified (Codex #7)
- Unit 5's "publication" = (1) build wheel, (2) commit to `dist/`, (3) git tag `v0.2.0` after merge
- NOT PyPI upload (out of scope; no credentials configured for that path)
- Unit 6 (backend rollout) installs the wheel by SHA pin from this commit

## 3. Recommended next action

Hand-write per iter-1 design + corrections A-F. ~1-2 hours (largest unit; corpus regen is mechanical, test infrastructure update needs care).

<promise>PLAN_ESCALATED</promise>
